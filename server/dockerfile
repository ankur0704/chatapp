FROM node:18

# Set working directory
WORKDIR /app

# Copy ONLY backend files (you must be inside server/ context)
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the rest of the backend code
COPY . .

# Expose backend port
EXPOSE 5000

# Start the server
CMD ["npm", "run", "server"]
